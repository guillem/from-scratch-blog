## Summary

- What does this change do?
- Does it include a schema migration? If yes, say so explicitly.

## Checklist

- [ ] `npm run ci` passes locally, or CI is green
- [ ] New migrations are generated (never edited after apply) and committed
- [ ] No secrets, tokens, site IDs, or personal posts were committed
- [ ] Docs were updated if architecture, commands, or operations changed
